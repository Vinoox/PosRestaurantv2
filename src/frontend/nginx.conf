server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Magiczna linijka dla aplikacji SPA (React/Angular/Blazor)
        try_files $uri $uri/ /index.html;
    }
}